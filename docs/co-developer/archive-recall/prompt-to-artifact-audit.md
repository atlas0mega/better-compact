# Prompt-to-artifact audit — OpenCode archive and recall

This is an evidence checklist, not a declaration of completion. Source of truth:
the current `OPENCODE` worktree and live host. The user's later corrections
supersede contradictory older numbers in the goal: automatic remains on, the
local target is 25% with a 15%-above-target last-resort tolerance, recent
reasoning falls back to 28k, seven Luna calls are shared with five attempts
per failed job, and raw archives/sidecars expire after seven days. Pi and
Claude Code retain their own behavior.

| Required outcome | Artifact / observed evidence | Still missing or uncertain |
| --- | --- | --- |
| Review pre-existing dirty core edits rather than reset them | `git status --short --branch`, `git diff --stat`, `git log --oneline`; `packages/core/src/{ladder,stages}.ts` and `tests/ladder.test.ts` were reviewed and committed as `99d23de` in draft PR #5. Current follow-up edits implement later user corrections. | Final review of follow-up diff before updating PR #5 |
| Diagnose assistant/tool-loop auto-compaction, privacy-safe entry/skip reasons | `packages/opencode/lib/hooks.ts` (`recordAutomaticCheck`, idle and pre-request transforms) persists seam, count, reason and token numbers, never prompt text; tests cover above-trigger no-human continuation and a 50-step loop. An isolated OpenCode 1.18.32 host with local Ollama made tool calls and a real above-trigger `pre_request` check reported `planned` with no intervening human turn. | Confirm the restarted target host/model follows the same seam and measure a non-capped provider response; isolated host evidence is not every branch |
| Respect model-specific triggers, permission ask/deny, subagent scope, terminal no-next-request | `better-compact.jsonc`; `lib/config.ts`, `lib/hooks.ts`, `lib/compress-permission.ts`, `tests/hooks-permission.test.ts` | Host-level cross-model acceptance, including a real continuing assistant/tool turn and terminal turn |
| One exact private ordered delta per advanced boundary, no replay/settings duplicate | `lib/boundary/archive-catalog.ts` (`archiveBoundaryDelta`, `uncoveredMessages`), `archive-catalog.test.ts` successive/revised/split/unchanged tests | Compare live source IDs/bytes to a real archive after final-build compaction |
| Crash-safe plan/catalog, checksum, private path and legacy range-transcript continuity | `lib/private-storage.ts`, `state/persistence.ts`, `boundary/transcripts.ts`, `archive-catalog.test.ts`, `private-storage.test.ts` | Killed-host recovery and legacy migration audit on a live resumed session |
| Keep nonarchived user messages byte-exact and first-round archived wording live | `core/src/stages.ts` (`prefixUserMessages`), `boundary/engine.ts` (`retainArchivedUserText`), `archive-retirement.test.ts`, `hooks-permission.test.ts`; `archive-catalog.test.ts` now proves a revised user payload with the same ID does not retire when only its old fingerprint is described | Check active native wording on a final-build real request |
| Retire only *older* described wording at a later validated boundary; preserve current corrections and failed/pending cohorts | `eligibleRetirementThrough` in `boundary/archive-catalog.ts`; auto and manual callers in `boundary/engine.ts`/`lib/hooks.ts`; round-one/round-two tests in `archive-retirement.test.ts` | Authorized live target-session round-two quality and provider-context observation; synthetic tests are insufficient |
| Preserve current decisions across checkpoint rollover and expire only absorbed per-turn caches | `core/src/{ladder,stages,plan}.ts`; `core/tests/ladder.test.ts` covers prior checkpoint, nonprefix replay, cache stability and oversized summaries. A new validated handoff releases redundant older native assistant turns even when older descriptions are still pending; their required exact user wording remains in the handoff until eligible retirement, and exact raw turns remain archived. Protected assistant/reasoning keys age out as newer outputs arrive. | Long real-session rollover and invalid/smaller-summary acceptance audit |
| Generated plugin prompts do not masquerade as human intent | `lib/messages/injection.ts`, `codec.ts`, `boundary/archive-summarizer.ts`; `boundary-context.test.ts`, `hooks-permission.test.ts` | Live provenance on sessions with plugin-generated continuations |
| Separate ≤100-word ready descriptions, only newest five live, older/pending by catalog | `archive-summarizer.ts`, `archive-catalog.ts` (`liveArchiveDescriptions`), `recall-tool.test.ts`, `archive-summarizer.test.ts` | Live host catalog display with more than five descriptions |
| Provider/model/effort, model-context-aware chunks, bounded seven-call shared budget, one fitting call, five failed-job attempts and no replay billing | `lib/boundary/{summarizer,archive-summarizer}.ts`, `tests/{archive-summarizer,summary-effort,prefix-chunks}.test.ts` | Earlier five-failed-Luna-call run's precise historical failure class remains unproven |
| Authorized successful live Luna/high handoff and Luna/default description | Approved isolated root-SDK run yielded one accepted high handoff and one ready 33-word default description (one provider prompt each; handoff 384 characters); command printed success, then timed out during shutdown. A later isolated Luna host run produced a ready description without a high handoff because cheap pruning was already sufficient. | Acceptance in the actual continuing target host/session, not just a synthetic scratch project |
| Decide summary acceptance by *complete* projected active context; preserve oversized failed summary and its associated original turns privately | `core/src/ladder.ts` rebuild comparison; `lib/hooks.ts`/`boundary/engine.ts` compare the whole accepted model plan against the exact cheap-pruned turns it would replace, including wrappers, protected parts, raw tail and overhead. On failure the OpenCode deterministic whole-prefix fallback is removed: keep cheap-pruned native context and prior validated checkpoint, archive the rejected summary as a permission-checked sidecar. `archive-retirement.test.ts` confirms a validated handoff beats the cheap baseline before intent retires; `hooks-permission.test.ts` covers an oversized candidate and old-snapshot migration. | Verify live current-intent quality and replacement size on a final-build provider request; rejected candidates must never masquerade as a validated handoff |
| Best-effort 25% target, 15% last-resort tolerance, **2–5** real assistant outputs plus intervening reasoning under **one** buffered allowance; preserve native parts and report irreducibles | `better-compact.jsonc`, `core/src/{ladder,plan,stages}.ts`, `boundary/report.ts`, `core/tests/ladder.test.ts`, golden/report tests. Two outputs are the minimum when available; if their full reasoning cannot fit, the single fallback has a 20k floor when feasible, up to the configured 28k, without stacking an extra reserve. Earlier protected keys expire as outputs advance. The no-growth guard rejects expanding plans. An isolated Luna host (test-only 2k trigger/target) reported 3,057 before, 1,486 projected, 1,965 after, `finish=stop`. The real target-session **old build** saved a 65,186 projection at 23:49:05 and the next populated provider result was 103,356 (~16%) at 23:49:50, below the 25% goal; a later 152k reading did not measure that initial event. | Measure first provider result after the final model-only build in the restarted target session; the earlier 385.4k observation replayed another old plan below its 429k trigger |
| Seven-day exact archive/sidecar expiry with tombstones (latest user instruction) | `archive-catalog.ts` (`ARCHIVE_RETENTION_MS`, `expireArchives`), `archive-catalog.test.ts` | Check no live/session lineage surprises on expiry; older goal text saying indefinite retention is superseded |
| Bounded startup description backfill without project-wide scanning | `projectArchiveRoots` checks only the opened project's `.opencode/better-compact/sessions`; `archive-catalog.test.ts` verifies nested pending archives wait until that nested project is opened and symlinked roots are rejected | Observe startup backfill against a real pending catalog in the opened project |
| Session-scoped low-prominence catalog/excerpt/page recall with host read permission, lineage, realpaths, abort, bounded output, byte-exact pages | `lib/tools/recall.ts`, `archive-catalog.ts`, `recall-tool.test.ts` (including UTF-8 emoji excerpts and exact-file `always` scopes); SDK 1.17.13 types declare scoped `ask`. On an isolated OpenCode 1.18.32 host with local Ollama and a synthetic catalog, the registered tool executed once with host `read: deny` and returned `permission_denied`, then executed with host `read: allow` and returned the catalog ID. Per-request `tools.read` was omitted so it could not override the configured host rule. | Actual host paged/excerpt recall under allow and denial on any sensitive path remains unobserved; registration plus a catalog call alone is narrower than full mode acceptance |
| Content-validated fork links and separate subagent catalogs | `boundary/context.ts`, `archive-catalog.ts` (`inheritArchiveCatalog`), `recall-tool.test.ts`, `hooks-permission.test.ts`. OpenCode 1.18.32 fork source creates title `original (fork #N)` without `parentID`; inheritance now requires that host title relationship plus the existing prefix content fingerprint. A separate session with identical prefix but no fork title is denied. | Actual host fork/subagent lineage check; title+content cannot cryptographically prove parenthood if both are deliberately forged |
| TUI queue correlates a pending request with its eventual server job | `lib/tui/data.ts` (`queuedBoundaryJob`), `lib/hooks.ts`, `tests/tui-progress-channel.visual.tsx`, `tests/tui-command.visual.ts` | Observe a long real busy-turn queue after reloading the final build |
| Workspace/adapter compatibility and final deliverable | Follow-up source run: `pnpm -r test` (core 95, OpenCode 158, Pi 82, CLI 43), `pnpm -r typecheck`, `pnpm -r build`, `pnpm -r verify:package`, isolated `pnpm test:tui` (20), and `git diff --check` all passed after the latest code edit. Draft PR #5 is open from `OPENCODE` to `main`, unmerged. | Review follow-up diff, update PR #5, and obtain final-build target-session post-provider and handoff evidence. No GitHub checks reported yet |

No row whose final gate is still listed constitutes complete-goal evidence.
